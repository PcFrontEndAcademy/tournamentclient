/*
 * TournamentDetails Messages
 *
 * This contains all the text for the TournamentDetails container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.TournamentDetails';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TournamentDetails container!',
  },
});
