/*
 * EliminationRoundPage Messages
 *
 * This contains all the text for the EliminationRoundPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.EliminationRoundPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the EliminationRoundPage container!',
  },
});
