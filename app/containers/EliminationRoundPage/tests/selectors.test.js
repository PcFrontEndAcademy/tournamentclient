// import { fromJS } from 'immutable';
// import { selectEliminationRoundPageDomain } from '../selectors';

describe('selectEliminationRoundPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
