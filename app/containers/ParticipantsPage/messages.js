/*
 * ParticipantsPage Messages
 *
 * This contains all the text for the ParticipantsPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ParticipantsPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ParticipantsPage container!',
  },
});
