/*
 * TeamsPage Messages
 *
 * This contains all the text for the TeamsPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.TeamsPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TeamsPage container!',
  },
});
