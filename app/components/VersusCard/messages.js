/*
 * VersusCard Messages
 *
 * This contains all the text for the VersusCard component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.VersusCard';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the VersusCard component!',
  },
});
