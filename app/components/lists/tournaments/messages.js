/*
 * Tournaments Messages
 *
 * This contains all the text for the Tournaments component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.Tournaments';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Tournaments component!',
  },
});
